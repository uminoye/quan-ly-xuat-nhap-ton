fix(report): group revenueByMonth by correct time unit per period

For revenueByMonth chart:
- quarter/all: group by YYYY-MM (month) — shows monthly breakdown within quarter/year
- day/week/month: group by YYYY-MM-DD (day) — shows daily bars within the period

Previously revenueByMonth always grouped by month regardless of period,
so quarter showed the same 12 months as all.
